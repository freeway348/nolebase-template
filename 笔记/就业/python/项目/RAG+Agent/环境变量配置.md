# 环境变量配置

为避免API KEY泄露，我们可以通过环境变量来隐藏明文API KEY，OPENAI_API_KEY表示用于openai库的api，DASHSCOPE_API_KEY表示用于langchain库的api，值为实际的API KEY
![](assets/Pasted%20image%2020260317131627.png)

当代码中没有显式API KEY时，会自动调取默认的环境变量值（即以上两个环境变量设置）