# 01_python基础
### 一、python基础语法介绍

```
python是一种解释型语言
```
#### 1. 命名规则

> python定义变量的同时必须赋值，如果暂时不用，则需要先赋空值
1. 驼峰命名
	1. 大驼峰：每个单词首字母大写，例如： MyName （一般用于类名）
	 2. 小驼峰：第二个单词开始首字母大写，例如： myName （一般用于项目命名）
2. 下划线命名
	- 每个单词之间使用下划线连接，例如： my_name （常用于变量命名）
#### 2. 常用快捷键

1. 复制并粘贴多行代码：`ctrl + d`
2. 格式化代码：`ctrl + alt + L`（统一对齐代码，使代码整齐好看）
3. 单行注释：`ctrl + /`
#### 3. 数据类型

```python
# 查看数据类型
type(数据/变量名) # 配合print可输出对应 数据/变量名 的数据类型
```
常见数据分为如下两类：
1. 数字型
	1. 整型
	2. 浮点型
	3. 布尔型：非0为真，0为假
2. 非数字型
	1. 字符串(str)（多行注释里的内容也是字符串） ---- 非空为True，空为False
	2. 列表(list)：\[11,22,"aa"\]
	3. 元组(tuple)：(11,"aa",22)
	4. 集合(set)：{11, 22, 33}
	5. 字典(dict)：{"key1": "value1", "key2": "value2"}

> 区分 可变数据类型 与 不可变数据类型 的关键在于：数据在内存中是否能被改变。
> 例如：列表创建后，可以随意更改此列表的数据，但该列表在内存空间中的物理地址不会变
##### 1）可变数据类型

以下类型属于可变数据类型：
1. 列表
2. 字典
3. 集合
###### 列表

- 列表一般定义为以下两种形式：
	1. 空列表
		- 在实际测试中经常需要定义空列表，存放常用的一些测试数据
		- 用法：
			1. 用 \[\] 定义：`列表变量名 = []`
			2. 类实例化方式定义：`列表变量名 = list()`
	2. 非空列表
		- 用法：`列表变量名 = [元素1， 元素2，...， 元素n]`
- 列表查询：
	1. 通过索引直接查询：字符串\[索引\]
	2. 使用count()方法查找列表中的指定元素，若返回-1，说明没有该元素，若返回值大于1，则说明有重复元素
		- 用法：列表变量名.count(指定元素)
```python
list1 = [1, 2, 3, 4, 5, 3, 6, 7, 8, 9, 10]
print(list1[0]) # 输出：1  
print(list1.count(1)) # 输出：1  
print(list1.count(3)) # 输出：2  
```
- 列表元素的增加：
	1. append()方法：在列表的末尾添加数据
		- 用法：列表变量名.append(需要添加的数据元素)
	2. insert()方法：在指定位置插入数据，该位置原数据及其后的数据均后移一位
		- 用法：列表.insert(指定插入位置，需要添加的数据)  ---- insert的参数一从下标0开始计数
```python
val_list = ["Web自动化", "UI自动化", "接口自动化"]  
val_list.append("app自动化")  
print(val_list) # 输出： ['Web自动化', 'UI自动化', '接口自动化', 'app自动化']  
  
val_list.insert(2, "测试开发")  
print(val_list) # 输出： ['Web自动化', 'UI自动化', '测试开发', '接口自动化', 'app自动化']
```
- 列表元素的删除：
	1. pop()方法：删除指定索引对应的数据，若不指定索引，则默认删除最后一个元素
		- 用法：列表.pop(索引)
	2. remove()方法：删除列表中的指定元素
```python
val_list = ["Web自动化", "UI自动化", "测试开发", "接口自动化", "app自动化"]

# 指定位置的元素删除  
val_list.pop(2)   # 删除了下标为2的数据 -- "测试开发"
print(val_list) # 输出： ['Web自动化', 'UI自动化', '接口自动化', 'app自动化']  
  
val_list.pop()   # 删除了末尾数据
print(val_list) # 输出： ['Web自动化', 'UI自动化', '接口自动化']  
  
# 指定元素的删除  
val_list.remove("UI自动化")  
print(val_list) # 输出： ['Web自动化',  '接口自动化']

val = ["11", "11", "22", "3"]  
val.remove("11")   # 只会删除匹配到的第一个元素
print(val) # 输出：['11', '22', '3']
```
- 列表元素的修改：
	1. 将新数据直接赋值给列表的指定索引位置
		- 用法：列表\[索引\] = 新数据
	2. reverse()方法：反转列表，将列表中的元素倒序
	3. sort()方法：列表排序
		- 用法：
			- 升序：列表.sort()
			- 降序：列表.sort(reverse=True)
```python
# 列表的修改  
# 列表反转  
list2 = [11, 32, 3, 5, 4]  
list2.reverse()  
print(list2) # 输出：：[4, 5, 3, 32, 11]  
  
# 列表的排序  
list2.sort() # 升序  
print(list2) # 输出：[3, 4, 5, 11, 32]  
  
list2.sort(reverse=True) # 降序  
print(list2) # 输出：[32, 11, 5, 4, 3]

# 列表的嵌套  
list3 = [  
    [1, 2, 3],  
    [4, 5, 6],  
    [7, 8, 9]  
]  
print(list3) # 输出：[[1, 2, 3], [4, 5, 6], [7, 8, 9]]  
# 获取 5 这个元素  
print(list3[1][1]) # 输出：5  
  
# 列表的合并  
list4 = [1, 2, 3]  
list5 = [4, 5, 6]  
list6 = list4 + list5  
print(list6) # 输出：[1, 2, 3, 4, 5, 6]
```

###### 如何存储更多测试数据？ -->  字典

> 字典的len()方法 -->  一个键值对 对应 一个长度
- 定义方式：
	1. 用{}定义空字典：`data = {}`
	2. 用{}定义字典：`data = {"name" : "admin", "pwd" : "123"}`，数据之间用英文逗号隔开
	3. 用类实例化方法定义：`data = dict()`
		- 字典的键一般使用字符串，并且<font color="#ff0000">键名不能重复</font>（否则重复数据会被覆盖）
- 字典的查询：
	1. dict.get("key")：通过键名查询对应的值(如果没有键，则返回None，不会报错)（更推荐）
	2. dict\["key"\]： 直接访问字典的键（该方法若访问不存在的键，则会直接报错）
```python
dict1 = {"name": "张三", "age": 18, "sex": "男"}  

# 字典元素的查询  
print(dict1.get("name")) # 输出：张三  
print(dict1.get("agee")) # 输出：None  
  
# 元素查询：变量名["键"]  
print(dict1["name"]) # 输出：张三  
#print(dict1["agee"]) # 报错
```
- 字典的新增：
	1. 🌟dict\["key"\] = value：若键不存在，则增加键值对（自动创建）；若键存在，则修改键对应的值
```python
dict1 = {"name": "张三", "age": 18, "sex": "男"}  

# 字典元素新增  
dict1["phone"] = "11453215322" # 增加新的键值对  
print(dict1) # 输出：{'name': '张三', 'age': 18, 'sex': '男', 'phone': '11453215322'}  
dict1["name"] = "李四" # 修改已存在键对应的值  
print(dict1) # 输出：{'name': '李四', 'age': 18, 'sex': '男', 'phone': '11453215322'}
```
- 字典的删除：
	1. del dict\["key"\]：删除指定键值对（该删除方法还会同时释放内存空间）
	2. dict.pop("key")：删除指定键值对（pop()方法必须有参数，字典的pop方法没有默认值，因此不填写参数会导致报错）
```python
dict1 = {"name": "张三", "age": 18, "sex": "男", 'phone': '11453215322'}  

# 字典元素的删除  
del dict1["phone"]  
print(dict1) # 输出：{'name': '李四', 'age': 18, 'sex': '男'}  
dict1.pop("sex")  
print(dict1) # 输出：{'name': '李四', 'age': 18}
```
- 🌟🌟🌟字典元素的遍历：
	- 遍历字典的Key：for key in dict.<font color="#ff0000">keys()</font>  -->  循环拿到字典的每个键名（遍历时的默认方法）
		- for key in dict -->  默认取字典的键名
	- 遍历字典的Value：for key in dict.<font color="#ff0000">values()</font>  -->  循环拿到字典的每个值
	- 遍历字典的Key和Value：for k,v in dict.<font color="#ff0000">items()</font> --> 循环拿到每个键值对
		- 循环中需要两个临时变量，k和v 分别存储字典的 key 和 value
```python
# 字典的遍历  (以下代码均能正确输出)
dict1 = {"name": "张三", "age": 18, "sex": "男"}  
for key in dict1:   # 默认调用 .keys()方法  
    print(key)  # 输出每个键名
  
for value in dict1.values():  
    print(value)   # 输出每个值
  
for k,v in dict1.items():  
    print(k,v) # 输出每个键值对
```
###### 集合

- 定义：集合中的数据是没有重复的，主要应用于列表中的数据去重，且集合是**无序**的
- 用法：
	1. 用()定义，数据之间使用英文逗号分隔：`set1 = {1,2,3}`
	2. 通过类实例化方式定义：`set2 = set()`  -->  空集合只能通过该方法定义
- 列表元素的去重：变量 = list(set(列表变量))
- 字符串的去重：
	1. 将字符串转为集合：变量 = set(字符串)
	2. 将集合变回字符串：`"".join(变量)`
		- join()方法的使用：将变量使用 字符串连接起来
```python
# 集合  
# 对列表实现去重  
info = [11, 22, 11, 33, 22, 44, 55]  
print(set(info))  # 输出：[33, 11, 44, 22, 55]  
print(list(set(info))) # 列表去重后变为集合，将其重新变回列表类型  

info1 = "aabbccdd"  
aa = set(info1)  
print(aa) # 输出：{'d', 'a', 'b', 'c'}，集合不考虑元素顺序  
bb = "".join(aa) # 通过join方法将集合变为字符串  
print(bb) # 输出：dabc
# 若使用 bb = "-".join(aa)，则说明要将aa中的字符使用 - 串起来，最后输出为：d-a-b-c
```


##### 2）不可变数据类型

- 除可变数据类型外的其他剩余数据类型都属于不可变数据类型
###### 元组

> 元组与列表一样，都可用于存储多个数据，不同之处在于：<font color="#ff0000">元组的元素不能改变</font>
- 定义：
	1. 用()定义，数据之间使用英文逗号分隔：
		1. user_info = ()
		2. user_info = ("zhangsan", 18, 3.75)
	2. 通过类实例化方式定义：`info_tuple = tuple()`
	- 元组中只包含一个元素时，需要在元素后添加逗号（括号可不写）：`data = (1, )`，否则就不是元组类型，而是字符串或数字类型了
- 元组的查询：
	- 索引方式查询：item = 元组\[索引\]
	- count()：tuple.count(目标数据)  -->  使用方法同列表的count()方法
```python
# 如何交换两个变量的值  
# 常规思路：设置一个空的中间变量  
num1 = 100  
num2 = 200  
tmp = num1  
num1 = num2  
num2 = tmp

# python元组思路：以解包的形式赋值并进行元素互换  
num1, num2 = num2, num1  
"""  
思路解析： 相当于创建了一个临时元组变量，假设为 aa过程： aa = (num2, num1)      num1 = aa[0]      num2 = aa[1]    """
```

###### 字符串

- 定义：使用引号（单、双、三引号）引起来的内容就是字符串
- 引用：通过索引（下标）获取字符串数据
	- 索引（下标）：数据在容器中的位置编号。对于字符串来说，就是字符在整个字符串的位置
- 假设字符串为：`str = "abcdefg"`，则对应的索引为：

| 字符串str |  a  |  b  |  c  |  d  |  e  |  f  |  g  |
| :----: | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
|  正数索引  |  0  |  1  |  2  |  3  |  4  |  5  |  6  |
|  负数索引  | -7  | -6  | -5  | -4  | -3  | -2  | -1  |
- 一般情况下，均使用正数索引（从0开始）；python也支持负数索引（-1 表示容器的最后一个数据）
- 使用索引可以获取容器中指定位置的数据。如：字符串\[索引\]。`print(str[0])  # 输出 a`
- 获取字符串长度：len(字符串)。如：`len(str)`可获取str的长度，为7

- 切片：获取一段指定的字符串
	- 用法：字符串\[开始位置下标(start) : 结束位置下标(end) : 步长\]
		- 步长默认为 1 
		- 结束位置下标对应的数据不会被截取到，即：截取范围为\[start, end\)
```python
str1 = "abcdefg"  
  
print(str1[1:4]) # 输出cde  
  
print(str1[:5]) # 从下标 0 开始切片输出到指定位置（下标5），输出结果为abcde  
  
print(str1[2:]) # 从下标 2 开始切片输出，默认输出到整个字符串的结尾，输出结果为cdefg  
  
print(str1[:]) # 输出整个字符串  
  
print(str1[::2]) # 从下标 0 开始切片，步长为2，输出结果为aceg  
  
print(str1[::-1]) # 从下标 0 处开始切片，步长为-1，输出结果为gfedcba  
# 等同于 print(str1[6::-1])，若 print(str1[6:0:-1])，则不会把 a 输出出来，因为终点位置下标的数据不会被切片截取，所以不写就行  
  
print(str1[6::-1])  
  
print(str1[5::-1]) # 从下标 5 处开始切片，步长为-1，输出结果为fedcba
```


查找字符 --> find()方法
- 作用：判断被查字符串是否存在于当前字符串中。如果存在，则返回该字符串在当前字符串中的下标，不存在则返回 -1
- 用法：字符串.find("目标字符串")
```python
# find()方法  
str1 = "hello world"  
print(str1.find("e")) # 输出下标 1  

print(str1.find("o")) # 输出下标 4
# 只能在str1中找到第一个匹配的字符串，后续若仍有匹配的字符串，也无法返回其对应的下标。若找不到，则返回-1 
  
print(str1.find("x")) # 输出 -1，表示在字符串str1中不存在要查找的该字符串  
  
print(str1.find("hello")) # 输出下标 0
```

去除两端空白字符 --> strip()方法
- 作用：将字符串两端的空白字符（空格、换行\n、制表符\t）去除，返回一个新的字符串
- 用法：字符串.strip()
```python
# 去空格  
str2 = "    itheima@qq.com"  
print(str2.strip()) # 输出：itheima@qq.com（不包含空格）
```

大小写转换 --> lower()/upper()
- 作用：将字符串转化为大/小 写，返回新的字符串
- 用法：字符串.lower()；字符串.upper()
```python
str1 = "Hello World"

# 大小写变换  
print(str1.upper()) # 输出 HELLO WORLD
print(str1.lower()) # 输出 hello world
```

字符串拆分 --> split()
- 作用：按照指定字符来分割字符串
	- 方法执行后返回的数据类型为<font color="#ff0000">列表(list)</font>
	- 不传入分割符时，**默认以空白字符进行拆分**
- 用法：列表变量 = 字符串.split(分割符)
```python
# split()方法  
str3 = "selec0t * from stu where id=001"  
print(str3.split()) # 输出 ['selec0t', '*', 'from', 'stu', 'where', 'id=001']
print(str3.split("where"))  # 输出 ['selec0t * from stu ', 'id=001']
```

其他方法
1. 判断字符串是否**全是**字母/数字（字符串中不能出现空格）
	1. 判断字符串是否全是字母：字符串.isalpha()
	2. 判断字符串是否全是数字：字符串.isdigit()
2. 判断某个字符出现的次数：字符串.count("指定字符")

#### 4. 输入与输出

1. 输出：`print(数据/变量)`
2. 输入：`变量 = input("提示信息")`
	1. 程序执行到`input()`函数会等待用户输入，因此最好给出提示信息
	2. `input()`函数接收用户输入信息后，一般需要保存到变量中，以便于后续使用
	3. 🌟 `input()`会将用户输入的任意数据都识别为**字符串**处理，需要运算时注意类型转换，例如：`num = int(input("请输入任意数字"))`

- 输出的几种方式
	1. 格式化输出：将程序的运行结果按照一定格式输出
	2. format实现字符串格式化输出
	3. f-string格式化输出
```python
name = "小王"  
age = 18 
 
# 1. 格式化输出  
print(name + "今年" + str(age) + "岁")  
  
# 2. f-string输出  
# 例：print(f"{name}说你好")，假设变量 name = "小李"，则会输出：小李说你好；也即输出将{}框住的变量代值输出了  
print(f"{name}今年{age}岁")  
  
# 3. format()函数输出  
print("{}今年{}岁".format(name, age))  
  
  
# 以上三种输出结果均为：小王今年18岁
```
#### 5. 强制类型转换

1. int(x)：可以将其他整数型变量转换为int型变量；但当变量为字符串时，该字符串必须全是<font color="#ff0000">数字</font>类型
2. float(x) ：可以将其他整数型变量转换为float型变量；但当变量为字符串时，该字符串必须全是<font color="#ff0000">数字</font>类型
3. str(x)：可以将任意类型的数据 x 转换为字符串类型
4. tuple(s)：将序列 s 转换为一个元组
5. list(s)：将序列 s 转换为一个列表

序列：是 Python 中一种有序、可索引、可迭代的数据类型，包括：

|     类型     |     示例      | 特点      |
| :--------: | :---------: | ------- |
| 列表 (list)  | \[1, 2, 3\] | 可变序列    |
| 字符串 (str)  |    "abc"    | 不可变字符序列 |
| 元组 (tuple) |  (1, 2, 3)  | 不可变序列   |
|  range 对象  |  range(5)   | 数字序列    |
|  字典的键/值视图  | dict.keys() | 可迭代视图   |

---

`tuple(s)` 的使用示例：
```python
# 1. 列表 → 元组
list_s = [1, 2, 3]
tuple(list_s)        # (1, 2, 3)

# 2. 字符串 → 元组（每个字符成为元素）
str_s = "abc"
tuple(str_s)         # ('a', 'b', 'c')

# 3. 元组 → 元组（复制一份）
tuple_s = (1, 2, 3)
tuple(tuple_s)       # (1, 2, 3)

# 4. range → 元组
tuple(range(3))      # (0, 1, 2)

# 5. 集合 → 元组（集合不是序列，但可迭代，也能转）
set_s = {1, 2, 3}
tuple(set_s)         # (1, 2, 3)  顺序不固定

# 6. 字典 → 元组（默认取键）
dict_s = {'a': 1, 'b': 2}
tuple(dict_s)        # ('a', 'b')
tuple(dict_s.values())  # (1, 2)
```

---

关键点总结

> 序列 s 的核心特征：可迭代（Iterable）+ 有序（Ordered）

- 只要是可迭代对象，`tuple()` 基本都能处理
- 转换时按原有顺序将元素逐个放入元组
- 字符串会被拆成单个字符
常见错误

```python
# 错误：传入非可迭代对象
tuple(123)           # TypeError: 'int' object is not iterable

# 正确做法：先转为字符串或其他可迭代类型
tuple(str(123))      # ('1', '2', '3')
```

#### 6. 常用运算符

1. \*：乘法 
2. /：除法  `print(5/2) # 输出2.5`
3. //：求商 `print(5//2) # 输出2`
4. \%：取余 `print(5%2) # 输出1`
5. != ：判断不等于的情况
6. 逻辑运算符：
	1. and：两个条件均满足才返回True，有一个不满足就返回False
	2. or：两个条件有一个满足就返回True，都不满足才返回False
	3. not（不能使用"！"替代）：如果原结果为True，则not后变为False，反之同理
- <font color="#ff0000">小括号优先级最高</font>。如果在复杂多重判断中不能确定运算优先级，加个小括号就好
```python
if score >= 70 and score < 80:
	print("良")
	
# 也可写作：
if 70 <= score < 80:
	print("良")
	
# 以上两种写法的作用一致
```
#### 7. pass关键词

pass在python中是一个占位符，如果没想好写什么代码，可以先试用pass代替，这样可以不报错

#### 8.多重判断语句

```python
if 条件1:
	条件1满足后的代码
elif 条件2:
	条件2满足后的代码
elif 条件3:
	条件3满足后的代码
......
else:
	以上条件都不满足，则执行此处代码
```
elif 和 else 必须配合 if 联合使用，不能单独使用
#### 9. 循环语句

1. while循环：可以指定循环次数，也可以做到死循环
```python
num = 1
while num <= 10:  
    print(f"吃掉了第{num}个苹果")  
    num = num + 1
# 会输出 "吃掉了第1个苹果""吃掉了第2个苹果"...."吃掉了第10个苹果"
```
2. for循环：可以配合`range()`方法实现循环特定次数，也可用于遍历容器中的值；for循环中也可使用`break`退出循环/遍历
	- 适用场景：针对有**容器**特征的数据进行遍历**获取数据**/**写入数据**的场景
		- 容器：非数字类型的数据，如：列表、元组、字典、集合  
```python
'''
for 临时变量 in range(循环次数):
	循环内部执行的代码
'''
# range(start, end + 1)：取值类型为整数，取值范围是[start, end + 1)，即：顾头不顾尾
# 例如： for i in range(1, 11)  -->  i 的取值会从1取到10
# 若为 for i in range(10) -->  整数取值范围为 [0,10)

# 循环用法
result = 0  
for i in range(1, 11):  
    result += i  
print(result)  # 输出55

# 遍历用法（从容器中取数据）
# 定义列表变量，分别取出列表中的每个元素  
# 容器：非数字类型的数据，如：列表、元组、字典、集合  
list1 = [1, 2, 3, 4, 5]  
for i in list1:  
    print(i)  
# 输出： 1 2 3 4 5
```
- **死循环**：由于循环条件始终满足，程序持续执行起来不会停止的现象
	- 应用场景：在一些循环次数不太明确的案例中可以使用死循环，但在死循环内部需要设置退出条件（使用`break`退出循环）。如：模拟密码登录案例，使用死循环不断模拟登录密码，若输入的密码正确，则退出死循环
- break关键字：可强制退出循环
```python
# 一共吃5个苹果，每次吃苹果都需要输出语句。但吃到第三个苹果就吃饱了，所以吃完第三个苹果就结束循环  
num = 1  
while num <= 5:  
    print(f"吃掉了第{num}个苹果")  
    if num == 3:  
        print("吃饱了")  
        break  
    num = num + 1  
# 结束
```
#### 10. 转义字符

1. `\n`：换行
2. `\t`：缩进
3. `\\`：输出反斜杠 \  (防止 \ 与其后的字母组成反义字符)
4. `\'`：单引号（同上）

#### 11. 函数

- 定义：把具有独立功能的代码块组织为一个小模块，在需要的时候调用
- 使用步骤：
	1. 定义函数：封装独立的功能
	2. 调用函数：享受封装的成果
- 作用：对具备相同逻辑的功能进行封装，提高代码的编写效率，实现对代码的重用
- 函数的返回值：如果想要获取函数定义内部的某个数据，需要使用`return`关键字返回结果
	1. 如果函数没有设置返回值，则函数调用的结果都是None
	- 如果在函数内，若 return 后还有语句，这些语句不执行
#### 12. 模块和包
##### 1）模块

- 每一个以`.py`结尾的Python代码文件都是一个模块
- 模块名同样也是一个标识符，需要符合标识符的命名规则
- 在模块中定义的 **全局变量**、**函数**、**类** 都是提供给外界直接使用的 **工具**
- 模块好比是工具包，要想使用这个工具包中的工具，就需要先 导入模块
- 导入方法：
	1. import 导入模块
	2. from 模块 import 导入工具
```python
# 获取1~10之间的随机数  
import random # 导入整个模块  
# 使用工具  
rage = random.randint(1, 10)  
print(rage)  
  
  
# 直接导入模块内的某个指定工具  
from random import randint  
rage1 = randint(11, 30)   # 由于只导入了模块内的指定工具，所以直接使用该工具也能知道random模块下的
print(rage1)

# 也可以直接导入自己写的 .py 文件
import hm01_login # 假设有login(phone, pwd)函数，用于判断账号密码是否正确
login("15011111111", "123456")
```
##### 2）包

- 包(Python Package)：包含多个模块的特殊目录
	- 目录下有一个特殊的文件`_init_.py`
	- 包名的命名方式和 **变量名** 一致
- 作用：python 文件较多时，**方便分目录管理维护**
- 导包的常用方式：
	1. import 包名.模块名
		- 使用：包名.模块名.工具名
	2. from 包名 import 模块名
		- 使用：模块名.工具名
	3. from 包名.模块名 import 工具名
		- 使用：工具名
#### 13. 调试代码的使用

- 通过使用`if __name__ == '__main__':`，将要调试的代码置于其下。这样做，当执行的不是当前文件时，导包文件的 `if __name__ == '__main__'`下的代码不会执行
	- 只有当前执行文件的 `__name__` 是 `__main__`，被导包的模块 的 `__name__` 就是模块名
#### 14. 引用

- 在计算机内存中，**变量** 和 **数据** 是分开存储的。变量 = 数据，即是将变量指向了数据所在的内存物理地址。使用 `id()` 方法可以查看 变量 中保存的 数据 所处的物理地址
- **数据** 保存在内存中的一个位置
- **变量** 中保存着数据在内存中的地址
- **引用**：变量中记录数据的地址，就叫做 **引用**
	- 当给一个变量重新赋值，本质上是对该变量的 引用数据地址 进行了修改
```python
age = 18
print(id(age))
age1 = 18
print(id(age1))

# 这两个变量存储的物理地址是一样的，都指向数字 18 所在的内存物理地址，唯一的区别是：这两个变量的物理地址不一致
```

- 在python中，函数的 **参数** 和 **返回值** 传递的都是引用信息。![](assets/Pasted%20image%2020260411154339.png)

#### 15. 局部变量与全局变量

```
若在函数内使用一个未定义过的变量，会先在函数内部查找，若函数内部没有该变量，则到函数外部进行查找，若外部也没有，再报错。
若同时有 相同定义名 的 局部变量 和 全局变量，系统会优先使用 局部变量，若函数内无 该局部变量，则使用全局变量 
```
- 局部变量：在函数内部定义的变量
- 全局变量：在函数外部定义的变量
##### 1）局部变量

- 局部变量 只能在函数内部使用，不同的函数内部可以定义相同的变量名，互不影响
- 函数执行完毕后，局部变量 会被系统回收
- <font color="#ff0000">生命周期</font>：
	- 局部变量 在函数执行时才会被创建
	- 函数 执行结束后，局部变量 会被系统回收
	- 局部变量在其生命周期内，可以用来 存储函数内部临时使用到的数据

##### 2）全局变量

- 如果需要修改全局变量，需要先在函数内部使用`global`关键字声明该全局变量
	- 在修改全局变量前的函数中，全局变量仍为原先值，在后边的函数中，全局变量为修改后的值
```python
# 全局变量  
num = 100  
  
def demo1():  # 全局变量为修改前的值
    print(num)  
def demo2():  # 修改全局变量
    global num  
    num = 101  
    print(num)  
def demo3():  # 全局变量为修改后的值
    print(num + 5)  
  
demo1() # 输出：100  
demo2() # 输出：101  
demo3() # 输出：106
```
#### 16.缺省函数

- 缺省函数：定义函数时，给某个参数指定一个默认值，具有默认值的参数。
	- 注意：有默认值的参数必须放在其他形参后，即：放在最后
- 说明：
	- 调用函数时，若无传入参数，则使用默认值
	- 将常见的值设为参数的缺省值，从而**简化函数的调用**
```python
# 使用一个函数定义学生的信息  
def student_info(name, age, sex="男"):   # sex默认值为 "男"
    """定义学生的信息"""  
    print("姓名：%s，年龄：%d，性别：%s" % (name, age, sex))  
  
  
if __name__ == "__main__":  
    student_info("张三", 18) # sex参数有缺省值，不传入参数则使用默认值  
    student_info("李四", 19, "女")
```
#### 17. 多值函数

- 说明：如果一个函数能够处理的参数个数是不确定的（可以为0个），可以考虑使用多值函数
- 如果要传入的数据是元组类型，则需要先对元组类型的数据进行<font color="#ff0000">拆包</font>
```python
# *args 用于接收元组类型数据，args为习惯命名，可自定义
# 多值参数一般放在固定参数后
def demo(num, *args) # *args 就是多值参数
	print(num)
	print(args) # 输出的是一个元组
	total = 0
	for i in args:  # 依次取出 args 中的数据
		total += i
	return total
	
if __name__ == '__main__'
	demo(1, 2, 3, 4, 5)
	nums = 1,2,3,4,5 # nums 是元组类型
	demo(nums) # 会报错，相当于把整个元组当成一个参数传递进去了，但*args 只支持一个一个的数据传输，不能一次性传入大量数据
	
# 如何处理 --> 元组的拆包 --> 在元组变量前加一个 * 
	demo(*nums) # 对元组数据拆包，正确传入，不会报错
```
#### 18. 匿名函数 -- lambda 表达式

- 使用 lambda 关键字创建的简化型函数，不同于使用def声明的函数，匿名函数没有函数名
- 匿名函数可以接收任意数量的参数，但只能返回一个表达式的值
```python
  
sum = lambda num1, num2 : num1 + num2  
  
def sum_numbers(num1, num2):  
    return num1 + num2  
    
if __name__ == '__main__':  
    print(sum_numbers(10, 20))  # 输出结果相同
    print(sum(10, 20))
```
- 作用：
	1. 简化函数定义
	2. 作为其他函数的参数使用
##### sort方法的拓展使用

sort(key, reverse)：通过对key参数进行设定，可以按照列表中某个元素的某项指标进行排序，key形参处必须传递 **函数类型** 的参数
```python
user_list = [  
    {"name": "小王", "age": 19, "sex": "男"},  
    {"name": "小李", "age": 28, "sex": "女"},  
    {"name": "小刚", "age": 32, "sex": "男"}  
]  
  
  
if __name__ == '__main__':  
    aa = lambda x : x["age"]  
    user_list.sort(key=aa, reverse=True) # sort()方法会自动循环列表元素，将每次循环的列表元素传递给形参x，并返回一个排序后的列表  
    print(user_list) # 按年龄降序排序
```

#### 19. 异常

- 异常：Python 程序在执行过程中，如果Python 解释器遇到一个错误，则会停止程序的执行，并提示错误信息
	- 程序停止执行，并提出错误信息的这个操作，通常称之为 **抛出(raise)异常** 
```python
'''
用法：
try:
	# 尝试执行的代码
except:
	# 出现异常时执行的代码
'''
try:  
    num = input("请输入数字：")  
    num = int(num)  
    print(num)  
except:  # 若输入为全数字，则无异常，以下代码不执行；若捕获到任意异常，则执行以下异常处理语句
    print("请正确输入数字")

'''
捕获特定异常：
try:
	# 尝试执行的代码
except 异常类型:
	# 出现异常时执行的代码

'''
try:  
    num = input("请输入数字：")  
    num = int(num)  
    print(num)  
except ValueError:  # 若输入为全数字，则无异常，以下代码不执行；若捕获到数据异常，则执行以下异常处理语句
    print("请正确输入数字")

'''
捕获未知类型的异常：
try:
	# 尝试执行的代码
except Exception as e:
	# 出现异常时执行的代码
	print(f"异常信息为{e}")
'''
try:  
    num = input("请输入数字：")  
    num = int(num)  
    print(num)  
except Exception as e:  
    print(f"错误信息为：{e}")
```
- 抛出异常信息的动作通常是Python解释器遇到错误时自动抛出的。
	- 如果想要人为主动抛出异常，就需要使用 Exception 异常类
		- 一般在开发过程中使用，先创建一个`Exception`类的对象，再使用`raise`关键字抛出异常对象
```python
# 法1
# 创建异常对象
ex = Exception("异常描述信息")
# 抛出异常对象，注意要在 try 中抛出异常，否则仅在报错信息中显示
raise ex

# 法2
# 也可使用下行代码主动抛出异常
raise Exception("异常描述信息")
'''
以下是示例代码
'''
def input_pwd():  
    try:        pwd = input("请输入密码：")  
        if len(pwd) > 4:  
            return pwd  
        else:  
            raise Exception("密码长度不够")  
    except Exception as e:  
        print(f"错误信息为：{e}")  
  
if __name__ == '__main__':  
    pwd = input_pwd()
```
#### 20. 🌟🌟🌟面向对象

- 面向对象：每个对象负责其功能模块，如果遇到需要执行某个功能的情况，就调用对应的对象去执行
	- 例如：有个 老师对象，其属性有：姓名，功能为：出考卷、批改试卷
- 为什么使用面向对象编程？  -->  为了解决复杂问题
##### 1）类与对象

- 类：具有相同特征和行为的事物的统称。是抽象的，不能直接使用的。类有**三个要素**：类名（大驼峰命名法）、属性（同类事物具备的特征）、方法（同类事物具备的行为）
	- 特征（静态）--> 属性
	- 行为（动态）--> 方法
- 对象：是由类创建的一个具体存在事物，可以直接使用
###### a.类与方法的定义

- 一个类可以创建多个对象，每个对象在内存中都有其独立的空间，每个self记录对象内存空间地址（简单来说：self就是当前调用对象）
```python
# 定义类和方法：

# Python2.x版本的要求
class 类名(object):
	# 方法与函数的唯一区别：方法必定有self参数，函数没有
	def 方法名(self, 参数列表):
		pass

# python3.x版本的要求
# 在python 3.x版本中， class 类名   等同于  class 类名(object)
class 类名:
	def 方法名(self, 参数列表):
		pass
```
###### b.创建对象

- 在Python中，由类创建对象的过程就称之为<font color="#ff0000">实例化</font>
- 若要调用类中的方法，需要通过`对象.方法名()`来使用
- 使用类创建对象时，每个对象的地址不一样
- 语法格式
```python
# 创建对象也叫：实例化
对象变量名 = 类名()
# 调用方法
对象变量名.方法名()
```
###### c.对象与内存

- 对象内存图：![](assets/Pasted%20image%2020260413150857.png)
- 可以使用`id()`来查看对象的内存物理地址
###### d.self

- self：当前<font color="#ff0000">对象</font>的内存地址引用
- 应用：在封装的类中，通过self访问当前对象的属性和方法
###### e.内置方法 -->  \_\_init\_\_方法与 \_\_del\_\_方法

```
python中，__表示内置函数/属性，例如：__init__ 是 python的内置方法
```
-  \_\_init\_\_方法：
	- 特点：创建对象时，**自动运行**该方法
	- 作用：一般用于**定义一个类具有哪些属性的方法**
- \_\_del\_\_方法：
	- 特点：对象运行结束后，会自动运行del方法
	- 作用：一般用于声明在对象被删除前需要处理的资源回收操作
```python
class Cat:  
    def __init__(self, name):  
        # 必须使用self.属性名 来指明该属性是属于哪一个对象  
        self.name = name  # 属性名 = 属性值  
    def eat(self):  
        print(f'{self.name}吃吃吃')  
  
    def drink(self):  
        print(f'{self.name}喝喝喝')  
  
  
if __name__ == '__main__':  
    # 创建对象  
    c1 = Cat("Tom")  
    c2 = Cat("大黄")  
    c1.eat()  # 输出：Tom吃吃吃  
    c2.drink()  # 输出：大黄喝喝喝  
    print(c1.name) # 输出: Tom  
    print(c2.name) # 输出：大黄
```
##### 2）面向对象的三大特征

- 面向对象有如下三大特征（测试一般只需要会前两者即可）：
	1. 🌟**封装**：根据需求将属性和方法封装到一个抽象类中   -->  方便管理
	2. **继承**：实现代码的重用，相同的代码不需要反复编写    -->  提高效率
	3. **多态**：不同的对象调用相同的方法，产生不同的执行结果，增加代码的灵活度  -->  增强灵活性
###### 继承

- 子类继承父类后，可以直接使用父类中已经封装好的属性和方法，所有的类都默认继承Object类
- 继承可以增加代码复用性，提高代码可维护性
- 语法要求：
```python
class A:
	def __init__(self):
		self.name = "父类"
	def desc(self):
		print("我是父类")

# 继承的语法：class 子类(父类)
class B(A):
	pass
'''
B类是A类的 子类，A类是B类的 父类，B类从A类继承
B类是A类的 派生类，A类是B类的 基类，B类从A类派生
'''

class C(B):
	pass
# C类又从B类继承，所以C类也会有A类的属性和方法，这就是 类继承的传递性
```
- 父类有时候也被称为：**基类**、**超类**
###### 方法重写

> 若父类中的方法无法满足子类需求时，可以对父类中的方法进行重写（override）

- 方法重写的种类：
1. 覆盖式重写：相当于在子类中定义了一个和父类同名的方法并实现
	- 应用场景：父类与子类的方法实现完全不同
```python
# 定义动物类，动物有姓名和年龄属性，具有吃和睡的行为  
# 定义猫类，猫类具有动物类的所有属性和方法，并且具有抓老鼠的特殊行为  
  
  
class Animal:  
    def __init__(self, name, age):  
        self.name = name  
        self.age = age  
    def eat(self):  
        print("吃吃吃")  
  
    def sleep(self):  
        print("睡睡睡")  
  
class Cat(Animal):  
  
    def eat(self):  
        print(f"{self.name}要吃鱼")  
    def catch_mouse(self):  
        print("抓老鼠")  
  
  
  
# 创建对象  
c1 = Cat("小猫", 2)  
c1.eat() # 会调用Cat类中重写后的eat方法，而不是父类的eat方法  
# 需求发生变化：猫要吃鱼
```
2. 扩展式重写
	- 实现步骤：
		1. 在子类中重写父类方法
		2. 在子类中需要的位置使用`super().父类方法名`调用父类方法执行
- super：在python中，super是一个特殊的类，super()是super类创建出的对象，super()默认指的是父类
```python
# 定义动物类，动物有姓名和年龄属性，具有吃和睡的行为
# 定义猫类，猫类具有动物类的所有属性和方法，并且具有抓老鼠的特殊行为


class Animal:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def eat(self):
        print("吃吃吃")

    def sleep(self):
        print("睡睡睡")


class Cat(Animal):

    def eat(self): # 重写的方法名仍然与父类方法名相同
        super().eat()  # 扩展式重写父类方法，会先执行父类的eat()方法，再执行扩写的内容
        print(f"{self.name}更喜欢吃鱼")

    def catch_mouse(self):
        print("抓老鼠")


# 创建对象
c1 = Cat("小猫", 2)
c1.eat()
# 需求发生变化：猫要吃吃吃，但是它更喜欢吃鱼

```
###### 多态（了解即可）

- 前提：**继承**和**重写**父类方法
- 多态：就是子类继承父类后，对方法进行重写，使得不同对象调用相同方法得到不同结果
##### 3）类对象、类属性与类方法

| 一般称呼 | python推荐称呼 |
| :--: | :--------: |
|  对象  |     实例     |
| 创建对象 |   类的实例化    |
| 对象属性 |    实例属性    |
| 对象方法 |    实例方法    |
- 类对象：类本身就是类对象。一个类只有一个类对象，定义完类自动创建，<font color="#ff0000">类名就是类对象</font>
- 类具备自己的属性和方法：**类属性** 和 **类方法**。可直接通过 **类名调用**
	- 类属性：该类所有对象的共有属性
	- 类方法：该类所有对象的共有方法
```python
class Demo:
	# 类属性定义
	name = "我是类属性"
	
	# @classmethod装饰器表示这是类方法（Java中是注解，Python中是装饰器）
	@classmethod
	def 类方法名(cls): # cls参数：是类的内存地址空间。性质与self一样，self是类创建的对象地址，而cls是类地址
		cls.name = "zhangsan" # 修改类属性值
# 调用
Demo.类方法名

# 只有在类方法中，才能使用 cls.类属性，在其他方法中，只能使用 类名.类属性
```
- 装饰器的作用：不改变原有函数定义和调用的基础上，增加额外功能
##### 4）私有属性与私有方法

- 定义：仅可在类定义内部被使用，无法被类外部的类对象、实例对象获取访问
- 语法：私有属性和私有方法前添加`__`
```python
class Demo:
	def __init__(self, name):
		# 私有属性
		self.__name = name
	# 私有方法
	def __run(self)
		print("正在跑步")
```
- 可以使用`dir()`查看对象的内置方法
### 二、Pytest
#### 1. 基本介绍

1. 什么是框架？
	- 框架（framework）是为了解决一类事情的功能技能，是一套工具集
2. 什么是Pytest框架？
	- Pytest是Python中的一种单元测试框架，相比UnitTest框架更简洁更高效
3. 为什么使用Pytest框架？
	1. 能组织多个用例执行
	2. 方便实现参数化
	3. 能够生成测试报告
#### 2. 定义测试用例

步骤：
1. 定义python文件，<font color="#ff0000">建议</font>以 test 开头
2. 定义测试类：<font color="#ff0000">必须</font>以Test开头
3. 定义测试方法：测试方法名称命名<font color="#ff0000">必须</font>以 test 开头
	- 建议格式：`test_数字_用例说明`
- 示例代码：![](assets/Pasted%20image%2020260413211138.png)
项目架构 ：![](assets/Pasted%20image%2020260414132350.png)
#### 3. Pytest执行测试用例

- 单条测试用例执行
	1. 命令模式：终端中输入`pytest -s test_xxx.py`
		- 例如，本案例中，要执行test_calc.py文件中的测试用例，需要在终端执行如下命令：`pytest -s .\script\test_calc.py`，这是因为终端默认位置是 D:\AutoTestLearning
		- 该命令可以同时执行指定文件下的所有测试用例
			- `-s`：允许使用 print 输出到终端
	2. 主函数模式：选择`test_xxx.py`文件直接执行（作了解）
- 批量用例执行
	- 通过pytest.ini配置文件实现
	- ![](assets/Pasted%20image%2020260414140421.png)
	- 配置完成后，只需要在终端使用`pytest`命令，即可执行指定文件夹下的指定用例
#### 4. 🌟断言

- 断言：让程序代替人工判断测试程序执行结果是否符合预期结果
- 作用：
	1. 提高测试效率
	2. 实现自动化测试
- 使用：在需要断言的地方之间使用断言关键字 --> assert
	- 若 assert 断言失败，则当前用例中在 assert 后的代码都不会执行
- 分类：
	- 相等关系：assert 预期结果  ==  实际结果  -->  即：实际结果和预期的值完全一致
	- 包含关系：assert 预期结果  in  实际结果  -->  即：实际结果包含预期的值
	- 真假关系：assert 实际结果  -->  即：实际结果为真
	- 近似相等：`assert (0.1 + 0.2) == pytest.approx(0.3)`
#### 5. 前后置操作

- 作用：对一个测试用例的前置和后置进行处理
- 控制级别：
	- 方法级别：在每个方法前后执行前后置操作
	- 类级别：在每个类前后执行前后置操作。例如：计算类中，在类前执行打开计算器操作，等待类中所有计算测试用例均执行完毕后再关闭计算器
	- 模块级别【了解】：在整个py模块前后执行前后置操作
##### 1）传统方式前后置
###### a.方法级别前后置

使用方法：
```python
class Demo:
	# 前置处理
	def setup_method(self):  # 首先自动执行
		pass
	# 后置处理
	def teardown_method(self): # 最后自动执行
		pass
	# 在每个测试用例执行前后，都必定执行一次前后置操作（若只写了前置处理，则只进行前置处理）
	def demo1(self):
		pass
```
###### b.类级别前后置

使用方法：在终端执行`pytest`命令
```python
class Demo:
	# 前置处理
	# 注意是 cls，而不是 self
	def setup_method(cls):  # 首先自动执行
		pass
	# 后置处理
	def teardown_method(cls): # 最后自动执行
		pass
	# 在第一个测试用例执行前进行前置处理，在最后一个用例执行后进行后置处理
	def demo1(self):
		pass
```
###### c.模块级别前后置

使用方法：在终端执行`pytest`命令
```python
# 在（第一个模块的第一个用例执行前）第一个方法用例执行前进行前置处理，在（所有模块均执行完毕后）最后一个用例执行后进行后置处理
# 前置处理
def setup_method():  # 首先自动执行
	pass
# 后置处理
def teardown_method(): # 最后自动执行
	pass
		
class Demo:
	def demo1(self):
		pass
```
##### 2）fixture前后置

- 基本用法：
	- 使用`@pytest.fixture`装饰器定义fixture函数
	- `yield`关键字前面的属于前置操作，后面的属于后置操作
	- 测试方法中引用`fixture`装饰器定义的函数名作为形参传入
- Fixture的作用域（scope）：
	- function(默认值)：每个测试函数执行一次fixture
	- class：每个测试类执行一次fixture
	- module：每个测试模块执行一次fixture
	- session：每个测试会话执行一次fixture
```python
import pytest

@pytest.fixture(scope="class")
def db_connection():
  # 模拟数据库连接
  print("开始连接数据库...")
  connection = "Database connection"
  yield connection         # 重点！！！
  # 模拟关闭数据库连接
  print("关闭数据库连接...")

class TestDemo:
  def test_db_query(self,db_connection): # 实际上，作为形参传入的db_connection是yield返回值的引用
    print("执行数据库的查询...")
    # 断言数据库连接成功
    assert db_connection is not None

```
- yield关键字：作用与`return`关键字类似，也能返回当前结果，等待该返回值被用完后，再继续执行后续代码，而不是直接结束
###### a.fixture代码的共享  -->  conftest

- `conftest.py`：是pytest的一个特殊配置文件，用于定义全局或局部的fixture。可以在不同的测试文件中<font color="#ff0000">共享fixture</font>，避免代码重复
- 作用域：
	- 当前目录：conftest.py  对同级目录及所有子目录的测试文件生效
	- 就近优先：子目录的  conftest.py  可以覆盖父目录同名 fixture，子目录的优先生效
	- 向上查找：pytest 会从测试文件所在目录向上逐层查找所有  conftest.py，直到找到当前使用的 fixture 所在位置
#### 6. Allure

```
使用pytest执行得到的测试结果只能在pycharm中看到，但我们还需要让同事、领导也看到测试结果，这就需要我们用到Allure代码生成测试报告
```
##### 1）基本介绍

- Allure能生成美观易读的报告，支持多种开发语言，如：java、python等，且能快速上手
- 操作步骤：
	1. 生成测试结果文件（json文件）
	2. 使用 allure 命令生成在线报告
- [帮助文档](https://docs.qameta.io/allure)：https://docs.qameta.io/allure
##### 2）环境配置

1. 前往https://github.com/allure-framework/allure2/releases下载allure
2. 解压缩到非中文目录下
3. 将allure的bin目录添加到环境变量中
4. windows命令行输入`allure --version`命令，能显示allure版本信息即为成功
- 在pycharm中安装：`pip install allure-pytest`
	- 查看是否成功安装：`pip show allure-pytest`
##### 3）使用步骤

1. 在`pytest.ini`配置文件中的命令行参数加上以下代码：`--alluredir report`
2. 编写完测试用例后，使用`pytest`命令执行用例
3. 运行结束后，会在项目的 report 目录下生成一些 json 文件
4. 使用命令`allure serve report`在线生成报告（基于report下的 json 文件）
	- 此处命令中的report指的就是前两步生成的report文件夹